{
  "name": "skopeo-ai-complete",
  "version": "1.0.0",
  "description": "Complete SKOPEO.AI system with LibreOffice and AI",
  "scripts": {
    "start": "node railway-start.js",
    "build": "cd auth-interface && npm install && npm run build",
    "start:complete": "./start-complete-system.sh",
    "stop:complete": "./stop-complete-system.sh",
    "install:all": "npm install && cd auth-interface && npm install && cd ../auth-backend && npm install",
    "build:frontend": "cd auth-interface && npm run build",
    "start:frontend": "cd auth-interface && npm start",
    "start:backend": "cd auth-backend && npm start",
    "start:wopi": "cd gpt && node server.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
